namespace Microsoft.HockeyApp.TestFramework
{
    using DataContracts;

    /// <summary>
    /// Required by Windows Runtime, which does not allow generics in public APIs.
    /// </summary>
    internal delegate void TelemetryContextAction(TelemetryContext context);
}
